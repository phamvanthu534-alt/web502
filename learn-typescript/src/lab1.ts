// 1. Viết 1 hàm trả về chu vi và diện tích hình chữ nhật
function chuViDienTichHCN(chieuDai: number, chieuRong: number) {
    const chuVi = (chieuDai + chieuRong) * 2;
    const dienTich = chieuDai * chieuRong;
    return { chuVi, dienTich };
  }
  
  console.log(chuViDienTichHCN(5, 10));
  
  
  // 2. Viết 1 hàm tính tổng nhiều số (không biết trước số lượng tham số)


  
  
  // 3. Viết hàm trả về số lượng xuất hiện của 1 kí tự trong chuỗi
  
  
  
  // 4. Viết hàm trả về boolean kiểm tra 1 số có phải số nguyên tố
